name: Dependabot auto-merge
on: pull_request_target

permissions:
  contents: write
  pull-requests: write
  checks: read

jobs:
  autoapprove:
    name: Auto Approve a PR by dependabot 
    runs-on: ubuntu-latest 
    steps:
      - name: Auto approve
        uses: hmarr/auto-approve-action@v2.0.0 
        if: ${{ github.actor == 'dependabot[bot]' }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
  automerge:
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'dependabot[bot]' }}
    steps:
      - name: Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v1.1.1
        with:
          github-token: '${{ secrets.GITHUB_TOKEN }}'
      - name: Wait for the PR build to pass
        if: ${{ steps.metadata.outputs.update-type == 'version-update:semver-patch' }}
        uses: lewagon/wait-on-check-action@v1.3.4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          check-name: build
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          wait-interval: 20
      - name: Merge Dependabot patch PRs
        if: ${{ steps.metadata.outputs.update-type == 'version-update:semver-patch' }}
        # The build check is already awaited above, so merge directly. Not using --auto:
        # `gh pr merge --auto` requires the repo to have "Allow auto-merge" enabled AND the
        # target branch to carry protection rules with required checks, otherwise GitHub's
        # enablePullRequestAutoMerge mutation fails with "Protected branch rules not configured".
        run: gh pr merge --squash "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
